{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "type": "stat",
      "title": "API readiness",
      "targets": [{"expr": "medflow_ready", "refId": "A"}],
      "gridPos": {"h": 6, "w": 6, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Requests by status",
      "targets": [{"expr": "sum by (status) (rate(medflow_http_requests_total[5m]))", "legendFormat": "{{status}}", "refId": "A"}],
      "gridPos": {"h": 8, "w": 18, "x": 6, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "API p95 latency",
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le) (rate(medflow_http_request_duration_seconds_bucket[5m])))", "legendFormat": "p95", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8}
    },
    {
      "type": "timeseries",
      "title": "Authorization decisions",
      "targets": [{"expr": "sum by (decision, reason) (rate(medflow_authorization_decisions_total[5m]))", "legendFormat": "{{decision}} {{reason}}", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["medflow", "production", "security"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timezone": "browser",
  "title": "MedFlow Production and Security",
  "uid": "medflow-production-security",
  "version": 1
}
